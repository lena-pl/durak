<p><strong><%= player_name(opponent) %> | Their Hand (<%= @game_state.player_state_for_player(opponent).hand.count %> cards):</strong></p>

<div class="player-hand">
  <% @game_state.player_state_for_player(opponent).hand.cards.each do |card| %>
    <div class="card"></div>
  <% end %>
</div>
